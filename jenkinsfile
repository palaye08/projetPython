pipeline {
    agent any
    
    environment {
        // Variables d'environnement
        DOCKER_HUB_REPO = 'palaye769/projetpython'
        DOCKER_HUB_CREDENTIALS = credentials('docker-hub-credentials')
        RENDER_DEPLOY_HOOK = credentials('render-deploy-hook')
        GIT_REPO = 'https://github.com/palaye08/projetPython.git'
    }
    
    stages {
        stage('Checkout') {
            steps {
                echo '🔄 Récupération du code source...'
                checkout scm
                
                script {
                    // Récupérer les informations du commit
                    env.GIT_COMMIT_SHORT = sh(
                        script: 'git rev-parse --short HEAD',
                        returnStdout: true
                    ).trim()
                    
                    env.GIT_BRANCH_NAME = sh(
                        script: 'git rev-parse --abbrev-ref HEAD',
                        returnStdout: true
                    ).trim()
                }
                
                echo "Branch: ${env.GIT_BRANCH_NAME}"
                echo "Commit: ${env.GIT_COMMIT_SHORT}"
            }
        }
        
        stage('Environment Setup') {
            steps {
                echo '⚙️ Configuration de l\'environnement...'
                sh '''
                    echo "Python version:"
                    python3 --version
                    
                    echo "Docker version:"
                    docker --version
                    
                    echo "Current directory:"
                    pwd
                    
                    echo "Files in directory:"
                    ls -la
                '''
            }
        }
        
        stage('Install Dependencies') {
            steps {
                echo '📦 Installation des dépendances...'
                sh '''
                    # Créer un environnement virtuel
                    python3 -m venv venv
                    
                    # Activer l'environnement virtuel
                    . venv/bin/activate
                    
                    # Mettre à jour pip
                    pip install --upgrade pip
                    
                    # Installer les dépendances
                    if [ -f requirements.txt ]; then
                        pip install -r requirements.txt
                        echo "✅ Dépendances installées"
                    else
                        echo "⚠️ requirements.txt non trouvé"
                        exit 1
                    fi
                '''
            }
        }
        
        stage('Run Tests') {
            steps {
                echo '🧪 Exécution des tests...'
                sh '''
                    # Activer l'environnement virtuel
                    . venv/bin/activate
                    
                    # Tests basiques
                    echo "Test: Import des modules principaux"
                    python3 -c "
import sys
sys.path.append('.')

try:
    # Test des imports principaux
    print('Test imports...')
    
    # Tests conditionnels basés sur la disponibilité des modules
    modules_to_test = [
        ('pandas', 'import pandas as pd'),
        ('flask', 'from flask import Flask')
    ]
    
    for module_name, import_cmd in modules_to_test:
        try:
            exec(import_cmd)
            print(f'✅ {module_name} importé avec succès')
        except ImportError as e:
            print(f'⚠️ {module_name} non disponible: {e}')
    
    print('✅ Tests d\\'import terminés')
    
except Exception as e:
    print(f'❌ Erreur lors des tests: {e}')
    sys.exit(1)
"
                '''
            }
            
            post {
                always {
                    echo '📊 Archivage des résultats de tests...'
                    // Archiver les logs de tests si ils existent
                    archiveArtifacts artifacts: 'test-results/**/*', allowEmptyArchive: true
                }
            }
        }
        
        stage('Build Docker Image') {
            steps {
                echo '🐳 Construction de l\'image Docker...'
                script {
                    // Construire l'image avec plusieurs tags
                    def imageName = "${env.DOCKER_HUB_REPO}"
                    def imageTag = "${env.GIT_COMMIT_SHORT}"
                    
                    sh """
                        # Build de l'image Docker
                        docker build -t ${imageName}:${imageTag} .
                        docker tag ${imageName}:${imageTag} ${imageName}:latest
                        
                        # Vérifier que l'image a été créée
                        docker images | grep ${env.DOCKER_HUB_REPO}
                    """
                    
                    env.DOCKER_IMAGE_TAG = imageTag
                }
            }
        }
        
        stage('Security Scan') {
            steps {
                echo '🔐 Scan de sécurité de l\'image Docker...'
                script {
                    try {
                        sh """
                            # Scan basique de l'image (si trivy est installé)
                            if command -v trivy &> /dev/null; then
                                echo "Scan avec Trivy..."
                                trivy image --exit-code 0 --severity HIGH,CRITICAL ${env.DOCKER_HUB_REPO}:${env.DOCKER_IMAGE_TAG}
                            else
                                echo "⚠️ Trivy non installé, scan de sécurité ignoré"
                            fi
                            
                            # Vérification basique de l'image
                            docker inspect ${env.DOCKER_HUB_REPO}:${env.DOCKER_IMAGE_TAG}
                        """
                    } catch (Exception e) {
                        echo "⚠️ Scan de sécurité échoué: ${e.getMessage()}"
                        currentBuild.result = 'UNSTABLE'
                    }
                }
            }
        }
        
        stage('Push to Docker Hub') {
            when {
                anyOf {
                    branch 'master'
                    branch 'main'
                }
            }
            steps {
                echo '📤 Push vers Docker Hub...'
                script {
                    docker.withRegistry('https://registry-1.docker.io/v2/', 'docker-hub-credentials') {
                        sh """
                            # Push des images
                            docker push ${env.DOCKER_HUB_REPO}:${env.DOCKER_IMAGE_TAG}
                            docker push ${env.DOCKER_HUB_REPO}:latest
                        """
                    }
                }
                
                echo '✅ Images poussées vers Docker Hub avec succès'
            }
        }
        
        stage('Deploy to Render') {
            when {
                anyOf {
                    branch 'master'
                    branch 'main'
                }
            }
            steps {
                echo '🚀 Déploiement sur Render...'
                script {
                    try {
                        sh """
                            # Déclencher le déploiement sur Render
                            curl -X POST "${env.RENDER_DEPLOY_HOOK}" \
                                -H "Content-Type: application/json" \
                                -d '{"ref": "${env.GIT_BRANCH_NAME}", "commit": "${env.GIT_COMMIT_SHORT}"}'
                        """
                        
                        echo '✅ Déploiement Render déclenché avec succès'
                        
                        // Attendre un peu pour que le déploiement commence
                        sleep(30)
                        
                    } catch (Exception e) {
                        echo "❌ Erreur lors du déploiement: ${e.getMessage()}"
                        currentBuild.result = 'FAILURE'
                        error("Déploiement échoué")
                    }
                }
            }
        }
        
        stage('Health Check') {
            when {
                anyOf {
                    branch 'master'
                    branch 'main'
                }
            }
            steps {
                echo '🩺 Vérification de l\'état de l\'application...'
                script {
                    try {
                        // Attendre que l'application soit déployée
                        echo "Attente du déploiement (2 minutes)..."
                        sleep(120)
                        
                        // URL de ton app Render (à adapter)
                        def appUrl = "https://projetpython.onrender.com"  // Remplace par ton URL
                        
                        sh """
                            # Test de health check
                            echo "Test de l'endpoint /health..."
                            
                            # Retry logic pour le health check
                            for i in {1..5}; do
                                if curl -f -s "${appUrl}/health"; then
                                    echo "✅ Application accessible"
                                    break
                                else
                                    echo "⏳ Tentative \$i/5 - En attente..."
                                    sleep 30
                                fi
                                
                                if [ \$i -eq 5 ]; then
                                    echo "❌ Application non accessible après 5 tentatives"
                                    exit 1
                                fi
                            done
                        """
                        
                        echo '✅ Health check réussi'
                        
                    } catch (Exception e) {
                        echo "⚠️ Health check échoué: ${e.getMessage()}"
                        currentBuild.result = 'UNSTABLE'
                    }
                }
            }
        }
    }
    
    post {
        always {
            echo '🧹 Nettoyage...'
            sh '''
                # Nettoyer les images Docker locales pour économiser l'espace
                docker system prune -f
                
                # Supprimer l'environnement virtuel
                rm -rf venv
            '''
        }
        
        success {
            echo '🎉 Pipeline exécuté avec succès!'
            
            // Notifications (optionnel)
            script {
                try {
                    // Slack notification (si configuré)
                    // slackSend(
                    //     channel: '#deployments',
                    //     color: 'good',
                    //     message: "✅ Déploiement réussi - Branch: ${env.GIT_BRANCH_NAME}, Commit: ${env.GIT_COMMIT_SHORT}"
                    // )
                    
                    echo "Notification de succès envoyée"
                } catch (Exception e) {
                    echo "Notification échouée: ${e.getMessage()}"
                }
            }
        }
        
        failure {
            echo '❌ Pipeline échoué!'
            
            // Notifications d'échec
            script {
                try {
                    // slackSend(
                    //     channel: '#deployments',
                    //     color: 'danger',
                    //     message: "❌ Déploiement échoué - Branch: ${env.GIT_BRANCH_NAME}, Commit: ${env.GIT_COMMIT_SHORT}"
                    // )
                    
                    echo "Notification d'échec envoyée"
                } catch (Exception e) {
                    echo "Notification échouée: ${e.getMessage()}"
                }
            }
        }
        
        unstable {
            echo '⚠️ Pipeline instable (certains tests ont échoué)'
        }
    }
}